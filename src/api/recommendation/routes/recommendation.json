{
    "routes": [
        {
            "method": "GET",
            "path": "/recommendation/:user_id",
            "handler": "recommendation.getRecommendations",
            "config": {
                "policies": []
            }
        }
    ]
}
